### Fixed

- Clicking a tab on the first-open debrief restores the grid, as Esc does: it kept the lap panel
  full-window, with no video, map or charts
- At a new circuit, the first open keeps the map and its start-line cue on screen; the debrief lands
  once the line is placed, and Save as track adds the PB line and the focus list
- Opening the demo no longer writes a Library row, a PB, a focus list or an Open Recent entry
