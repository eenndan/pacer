### Fixed

- On an unknown track the auto-fitted start line no longer follows a single GPS speed glitch; it
  sits where the speed stays highest, just before the braking point, so every lap is still counted
