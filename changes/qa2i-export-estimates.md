### Added

- The comparison video's options say how big the file will be and about how long it takes, as the
  lap export's do, from its own two-pane frame (#427)

### Fixed

- The export options' H.264 size estimate matches the file: it quoted VideoToolbox's target
  bitrate, and real files came out about a quarter smaller (#427)
