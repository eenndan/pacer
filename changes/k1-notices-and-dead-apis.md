### Fixed

- The session log now also keeps the failure notices that only reached a terminal: a refused
  reference, timing lines not saved, the GPS lag not applied, the IMU g refused, and more

### Changed

- The session log records what each load found: its laps, the g and rotation cross-checks, the
  GPS lag it corrected, and where the demo was downloaded from
