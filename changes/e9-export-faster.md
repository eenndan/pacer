### Changed
- Video export is 1.3-2x faster, every frame unchanged: MK's best lap renders in ~50 s at 4K (was
  ~100 s) and ~26 s at 1080p (was ~35 s); decode, overlay and encode now run side by side.
